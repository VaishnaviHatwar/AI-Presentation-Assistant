const chat=document.getElementById("chat"),form=document.getElementById("chat-form"),input=document.getElementById("message"),send=document.getElementById("send");let previousInteractionId=null;
function addMessage(text,role){const row=document.createElement("div");row.className=`message-row ${role}`;const bubble=document.createElement("div");bubble.className="bubble";if(role==="ai"){const safe=text.replace(/&/g,"&amp;").replace(/</g,"&lt;").replace(/>/g,"&gt;");bubble.innerHTML=safe.replace(/\*\*(.*?)\*\*/g,"<strong>$1</strong>").replace(/^###\s?(.*)$/gm,"<strong>$1</strong>").replace(/^##\s?(.*)$/gm,"<strong>$1</strong>").replace(/^#\s?(.*)$/gm,"<strong>$1</strong>");}else{bubble.textContent=text}row.appendChild(bubble);chat.appendChild(row);chat.scrollTop=chat.scrollHeight}
function typing(){const row=document.createElement("div");row.id="typing-row";row.className="message-row ai";row.innerHTML='<div class="bubble typing"><i></i><i></i><i></i></div>';chat.appendChild(row);chat.scrollTop=chat.scrollHeight}
async function sendMessage(message){addMessage(message,"user");typing();input.value="";input.style.height="auto";send.disabled=true;try{const r=await fetch("/api/chat",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({message,previousInteractionId})});const data=await r.json();document.getElementById("typing-row")?.remove();if(!r.ok){addMessage(data.error||"Something went wrong.","ai");return}previousInteractionId=data.interactionId||previousInteractionId;addMessage(data.text,"ai")}catch(e){document.getElementById("typing-row")?.remove();addMessage("I couldn't connect to the server. Make sure npm start is running.","ai")}finally{send.disabled=false;input.focus()}}
form.addEventListener("submit",e=>{e.preventDefault();const m=input.value.trim();if(m&&!send.disabled)sendMessage(m)});
input.addEventListener("keydown",e=>{if(e.key==="Enter"&&!e.shiftKey){e.preventDefault();form.requestSubmit()}});
input.addEventListener("input",()=>{input.style.height="auto";input.style.height=Math.min(input.scrollHeight,150)+"px"});
document.querySelectorAll(".examples button").forEach(b=>b.onclick=()=>{input.value=b.dataset.prompt;input.focus();input.style.height="auto";input.style.height=Math.min(input.scrollHeight,150)+"px"});